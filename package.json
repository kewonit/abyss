{
  "name": "abyss",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time network intelligence dashboard — Tauri app with MapLibre globe visualization",
  "scripts": {
    "dev": "cd tauri-host/src-tauri && cargo tauri dev",
    "build": "cd tauri-host/src-tauri && cargo tauri build",
    "build:release": "powershell -ExecutionPolicy Bypass -File scripts/build-release.ps1",
    "dev:vite": "cd visualizer && npm run dev",
    "install:all": "cd visualizer && npm install",
    "check:monitor": "node --check monitor.mjs",
    "check:visualizer": "npm --prefix visualizer run format:check && npm --prefix visualizer run lint && npm --prefix visualizer run typecheck && npm --prefix visualizer run build",
    "check": "npm run check:monitor && npm run check:visualizer"
  },
  "keywords": [
    "network",
    "visualizer",
    "tauri",
    "maplibre",
    "desktop-app"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kewonit/abyss.git"
  },
  "author": "",
  "bugs": {
    "url": "https://github.com/kewonit/abyss/issues"
  },
  "homepage": "https://github.com/kewonit/abyss#readme",
  "dependencies": {
    "ws": "^8.19.0"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "png-to-ico": "^3.0.1",
    "sharp": "^0.34.5"
  }
}
